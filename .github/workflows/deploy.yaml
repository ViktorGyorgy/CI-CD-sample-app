name: Deploy
on:
  push:
    branches:
      - '**'
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Installing node dependencies
        run: npm ci
